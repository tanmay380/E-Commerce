spring:
  data:
    mongodb:
      uri: mongodb+srv://tanmaysinghal380:tanmay@user-db.ggay0kn.mongodb.net/?appName=user-db
      database: userdb
server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  client:
    service-url:
      defaultzone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true