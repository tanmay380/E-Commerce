spring:
  mongodb:
    uri: ${URL}
    database: userdb
server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  client:
    service-url:
      defaultzone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true